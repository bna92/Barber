export interface TikTokVideo {
  id: string;
  user: string;
  profile: string;
  title: string;
  description: string;
  tags: string;
  video: string;
  link: string;
  active: boolean;
  order: number;
}